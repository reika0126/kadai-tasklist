<h1>id: <%= @task.id %>のタスク編集ページ</h1>

<%= from_with(model: @task, local: ture) do |f| %>
  <%= f.labe; :content, "タスク" %>
  <%= f.text_field :content %>
  
  <%= f.submit "完了" %>
<& end %>

<%=link_to "一覧に戻る", tasks_path %>